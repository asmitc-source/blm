/**
 * Local email/password sign-in (this app's Better Auth DB).
 *
 * Enabled for BLM workspace login/signup. Forms use `authClient.signUp.email` /
 * `authClient.signIn.email` from `@/lib/auth/client`.
 */
export const emailAndPasswordEnabled = true;
